@model IEnumerable<Dramonkiller.CareHomeApp.WebClient.Models.ResidentViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table" id="table-residents">
    <tr>
        <th>
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Middle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Surname)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <th>
                <img class="resident-img" data-id="@item.Id" style="height: 30px; width: 30px" src="~/Content/Img/male-user_32x32.png"/>
            </th>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Middle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Surname)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>

@section Scripts {

<script type="text/javascript">
    $(document).ready(function () {
        $(".resident-img").each(function () {
            var residentId = $(this).attr("data-id");
            var element = $(this);
            $.getJSON('http://localhost:16434/resident/' + residentId + '/photo')
              .done(function (data) {
                  if (!jQuery.isEmptyObject(data)) {
                      element.attr("src", "data:image/png;base64," + data);
                  }
              })
              .fail(function (jqXHR, textStatus, err) {
              });
        });
    });
</script>


}
